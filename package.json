{
	"author": "Chris DeFreitas <chrisd@europa.com>",
	"name": "objectLite",
	"dependencies": {
		"sqlite3": "^4.0.2"
	},
	"devDependencies": {
		"chai": "^4.3.4",
		"mocha": "^9.0.1",
		"stack-trace": "0.0.10"
	},
	"description": "Persistent Javascript object storage using sqlite and handy functions.",
	"license": "UNLICENSED",
	"repository": {
		"type": "git",
		"url": "https://github.com/ChrisDeFreitas/objectLite"
	},
	"scripts": {
		"test": "clear && node node_modules/mocha/bin/mocha --bail --check-leaks --use_strict test/test.js",
		"listtest": "clear && node node_modules/mocha/bin/mocha --bail --check-leaks --use_strict test/list-test.js",
		"misctest": "clear && node node_modules/mocha/bin/mocha --bail --check-leaks --use_strict test/misc-test.js",
		"compresstest": "clear && node node_modules/mocha/bin/mocha --bail --check-leaks --use_strict test/compress-test.js",
		"test?": "clear && node node_modules/mocha/bin/mocha --bail --check-leaks --prof --log-timer-events --use_strict --allow-uncaught --reporter dot",
		"console": "clear && node console.js debug",
		"gendocs": "clear && jsdoc -c jsdoc.json"
	},
	"version": "1.0.2"
}
